<%= form_with(model: book) do |form| %>
  <% if book.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(book.errors.count, "error") %> prohibited this book from being saved:</h2>
      <ul>
        <% book.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>
  <div>
    <%= form.label :publisher_id, style: "display: block" %>
    <%= form.collection_select :publisher_id, Publisher.where(active: true), :id, :name, include_blank: true %>
  </div>
  <div>
    <%= form.label :author_id, style: "display: block" %>
    <%= form.collection_radio_buttons(:autor_id, Autor.where(active: true), :id, :name) %>
  </div>
  <div>
    <%= form.submit %>
  </div>
<% end %>
